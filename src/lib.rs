pub(crate) mod connection;
pub mod participant_state;
pub mod thread_party;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
